<%= form_with(model: kitty) do |form| %>
  <% if kitty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kitty.errors.count, "error") %> prohibited this kitty from being saved:</h2>

      <ul>
        <% kitty.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
